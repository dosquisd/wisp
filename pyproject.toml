[project]
name = "wisp"
version = "0.1.0"
description = "Wisp — ephemeral WireGuard VPNs on your own cloud"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-aws>=7.0.0,<8.0.0",
    "pulumi-awsx>=3.8.0",
]
